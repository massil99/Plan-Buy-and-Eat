<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.planbuyandeat">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.PlanBuyAndEat">
        <activity android:name=".Identification.Signup"></activity>
        <activity android:name=".Identification.Login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Planning.Settings.Settings" />
        <activity android:name=".Planning.LDCView.LDCItems" />
        <activity android:name=".Planning.Planning" />
        <activity android:name=".Ingredients.Ingredients" />
        <activity android:name=".Repertoire.Repertoire" />
    </application>

</manifest>